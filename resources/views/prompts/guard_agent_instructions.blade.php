# Task

Check the user message and determine if it is violating any rules given below or is out (off-topic) of HelloWorld SaaS context.

Use "violationDetected" tool if it is violating any rules.

Important: if no rules are violated, return only "true".

{!! $rules !!}